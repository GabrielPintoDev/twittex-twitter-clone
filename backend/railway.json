{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn twitterclone.wsgi --log-file -",
    "healthcheckPath": "/api/explore/",
    "restartPolicyType": "ON_FAILURE"
  }
}
